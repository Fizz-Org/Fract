# Run a 'git pull' to make sure this is set to latest URL, not that that might still not update it, in which case, make sure you get the absolute latest version.
# Make sure it doesn't end in a "/".
ROOT_SERVER = "https://fract.fast-blast.uk"

# Enabeling this makes it so everything happens in working directory instead of default directories.
DEVELOPER_MODE = True

# Ignored if DEVELOPER_MODE is set to True, recommended not to edit.
CACHE_DIR = "~/.cache/fract"
